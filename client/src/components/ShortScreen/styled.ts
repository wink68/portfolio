import styled from "styled-components";

export const ShortScreenStyle = styled.div`
  height: inherit;
  display: block;
  
  font-weight: 600;
  color: while;
  text-shadow: 1vh 1vh 2vh rgba(0,0,0,0.2);
`;